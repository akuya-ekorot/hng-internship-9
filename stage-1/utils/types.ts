export type pageLink = {
  id: string;
  name: string;
  link?: string;
};

export type Social = {
  src: string;
  alt: string;
  link: string;
};

export type personalData = {
  twitter: string;
  slack: string;
  photo: string;
}