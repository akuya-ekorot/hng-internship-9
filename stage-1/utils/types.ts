export type pageLink = {
  name: string;
  link?: string;
};

export type Social = {
  src: string;
  alt: string;
};

export type personalData = {
  name: string;
  twitter: string;
  github: string;
  photo: string;
}